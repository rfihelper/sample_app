<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class='users'>
  <% @users.each do |user| %>
    <!-- renders the _users partial for each user in @users -->
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
